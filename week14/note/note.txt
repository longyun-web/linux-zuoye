Linux线程：可共享资源进程
1.内核线程
	在内核空间内执行线程的创建、调度和管理
	内核线程的创建和管理慢于用户线程的创建和管理
	特点：支持多处理器，支持用户进程中的多线程、内核线程切换的速度快
			对用户的线程切换来说，系统开销大
2.用户线程
	存在于用户空间中，通过线程库来实现
	县城创建和调度都在用户空进行，以线程为单位调度
	特点：统一进程内的线程切换不需要转换到内核
			系统调度阻塞问题，不能充分利用多处理器

同一进程中的线程切换
	由于共享许多资源，因此需保留的现场资源少；线程的切换不会引起进程的切换。

指针：32位的无符号整型
